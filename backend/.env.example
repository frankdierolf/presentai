# Backend Environment Variables Example
# Copy this file to .env and fill in your actual values

# ===========================================
# BACKEND DEPLOYMENT CONFIGURATION
# ===========================================

# OpenAI API Key for Realtime API (Required)
OPENAI_API_KEY=your_openai_api_key_here

# CORS Origins for frontend communication (Required for production)
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3030,http://127.0.0.1:3030,https://demo.presento.app

# Backend Server Port (Optional, defaults to 3000)
PORT=3000

# ===========================================
# DEPLOYMENT NOTES
# ===========================================

# Development:
# - Only OPENAI_API_KEY is required for local development
# - CORS allows localhost:3030 by default

# Production (Coolify):
# - Set OPENAI_API_KEY to your OpenAI API key
# - Set CORS_ORIGINS to include your frontend's deployed URL
# - Example: http://localhost:3030,https://demo.presento.app
# - Make sure CORS_ORIGINS includes all frontend deployment URLs

# Docker deployment will use these environment variables

# ===========================================
# CURRENT PRODUCTION SETUP
# ===========================================

# Based on your current deployment:
# Frontend URL: https://demo.presento.app
# Backend URL: https://backend.presento.app

# Required environment variables in Coolify backend deployment:
# OPENAI_API_KEY=sk-...your_actual_openai_api_key...
# CORS_ORIGINS=http://localhost:3030,http://127.0.0.1:3030,https://demo.presento.app