# Frontend Environment Variables Example
# Copy this file to .env and fill in your actual values

# ===========================================
# FRONTEND DEPLOYMENT CONFIGURATION
# ===========================================

# Backend API Base URL (Required for production)
# Point this to your backend deployment URL in Coolify
VITE_API_BASE_URL=https://your-backend-url.coolify.domain

# ===========================================
# DEPLOYMENT NOTES
# ===========================================

# Development:
# - Defaults to http://localhost:3000 if not set
# - No .env file needed for local development

# Production (Coolify):
# - Set VITE_API_BASE_URL to your backend's deployed URL
# - Example: https://presentai-api.your-domain.com
# - Make sure the URL matches your backend deployment exactly

# The frontend dist/ folder gets deployed as static files
# Environment variables are baked into the build at build time